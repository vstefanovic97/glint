import templateOnlyComponent from '@ember/component/template-only';

interface ReproductionSignature {
}

const TemplateOnlyModule = templateOnlyComponent<ReproductionSignature>();

export default TemplateOnlyModule;

